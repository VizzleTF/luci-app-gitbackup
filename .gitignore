.env
.env.*
!.env.example
node_modules/
__pycache__/
.DS_Store
.autopilot/serve.*

# owfeed build output (tools/stage.sh + owfeed build); owfeed.lock itself is committed
# per its own header ("Generated by owfeed. Commit this file.").
/dist/
dist/
package/*/dist/

# tools/fetch-eslint-config.sh's own scratch copy of openwrt/luci's
# eslint.config.mjs (pinned + checksummed in tools/luci-upstream.pin,
# re-fetched every run -- nothing to commit).
/.cache/
